<view class="navbar" style="{{'height: ' + navigationBarHeight}}">
  <view style="{{'height: ' + statusBarHeight}}"></view>
  <view class="status-bar" style="height:{{someData.statusBarHeight}}px"></view>
  <view class='title-container' style="height:{{someData.titleBarHeight}}px">
    <view class='capsule' wx:if="{{ back || home }}">
      <view>
        <image src='img/back.svg' bindtap='back' wx:if="{{back}}"></image>
        <image class="unlabeled" src='img/weibiaoji.png' bindtap="click"></image>
      </view>
    </view>
    <view class='title'>{{text}}</view>
  </view>
</view>